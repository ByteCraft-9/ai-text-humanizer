"""Shared inference library for the Python functions.

Modules here are prefixed with an underscore at the package level so Vercel
does not route them as endpoints — only top-level `api/*.py` files become
functions.
"""
